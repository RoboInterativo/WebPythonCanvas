version: '3.8'

services:
  python-vnc:
    build: .
    ports:
      - "5900:5900"

  novnc:
    image: theasp/novnc:latest
    ports:
      - "8080:8080"
    environment:
      - VNC_SERVER=python-vnc:5900
    depends_on:
      - python-vnc
